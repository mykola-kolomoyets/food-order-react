import React, { FC, Fragment } from 'react';
import './App.css';

import { Header } from '@components';

const App: FC = () => {
  return (
    <Fragment>
      <Header></Header>
    </Fragment>
  );
}

export default App;
